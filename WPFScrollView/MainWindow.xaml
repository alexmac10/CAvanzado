<Window x:Class="WPFScrollView.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPFScrollView"
        mc:Ignorable="d"
        Title="MainWindow" Height="300" Width="300">
    <ScrollViewer HorizontalScrollBarVisibility="Auto">
        <Canvas Height="500" Width="500">
            <Border Background="AliceBlue" Canvas.Left="50" Canvas.Top="70">
                <TextBlock Text="Hola"/>
            </Border>
            <Border Background="AntiqueWhite" Padding="30" Canvas.Left="92" Canvas.Top="21">
                <TextBlock Text="Mundo"/>
            </Border>
            <Border Background="Aqua" Padding="15" Canvas.Left="160" Canvas.Top="85">
                <TextBlock Text="Hello"/>
            </Border>
            <Border Background="Aquamarine" Padding="10,20,50,0" Canvas.Left="176" Canvas.Top="61">
                <TextBlock Text="World"/>
            </Border>
            <Border Background="Azure" Canvas.Left="28" Canvas.Top="36">
                <TextBlock Text="--Hola--"/>
            </Border>
            <Border Background="Beige" Canvas.Left="176" Canvas.Top="12">
                <TextBlock Text="--Mundo--"/>
            </Border>
        </Canvas>
    </ScrollViewer>
</Window>
